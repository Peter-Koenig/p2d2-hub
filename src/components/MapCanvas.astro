---
/// <reference types="vite/client" />
---

<div id="map" class="w-full h-full relative">
    <div id="crs-toggle" class="absolute top-2 right-10 z-10">
        <button
            id="crs-toggle-button"
            class="bg-white px-3 py-1 rounded-md shadow-md text-sm font-medium border border-gray-300 hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed"
            title="Koordinatensystem umschalten"
        >
            CRS: generisch
        </button>
    </div>
</div>

<script>
    // Import OpenLayers modules
    import Map from "ol/Map";
    import View from "ol/View";
    import TileLayer from "ol/layer/Tile";
    import OSM from "ol/source/OSM";
    import ImageLayer from "ol/layer/Image";
    import ImageWMS from "ol/source/ImageWMS";
    import FullScreen from "ol/control/FullScreen";
    import { defaults } from "ol/control/defaults";
    import { transform, transformExtent } from "ol/proj";

    // Import our utility modules
    import { mapState } from "../utils/map-state";
    import { CategoryHandler } from "../utils/category-handler";
    import { FeatureSelectionHandler } from "../utils/feature-selection";

    // Import existing CRS utilities
    import {
        registerUtm,
        toNewViewPreservingScale,
        isValidWgs84Coordinate,
        isValidWgs84Extent,
    } from "../utils/crs";

    // Throttled logging utility
    const createThrottledLogger = () => {
        const lastLogTimes = new Map();
        const throttleMs = 5000;

        return {
            warn: (key: string, message: string, ...args: any[]) => {
                const now = Date.now();
                const lastTime = lastLogTimes.get(key) || 0;

                if (now - lastTime > throttleMs) {
                    console.warn(message, ...args);
                    lastLogTimes.set(key, now);
                }
            },
        };
    };

    const throttledLogger = createThrottledLogger();

    // Initialize the map
    const map = new Map({
        target: "map",
        layers: [
            new TileLayer({
                source: new OSM(),
            }),
        ],
        view: new View({
            projection: mapState.getConfig().defaultCRS,
            center: [0, 0],
            zoom: 2,
        }),
        controls: defaults().extend([new FullScreen()]),
    });

    console.log("[MapCanvas] Map initialized successfully");
    console.log("[MapCanvas] Target element:", document.getElementById("map"));
    console.log(
        "[MapCanvas] View projection:",
        map.getView().getProjection().getCode(),
    );

    // Add WMS Layer
    const config = mapState.getConfig();
    const wmsLayer = new ImageLayer({
        source: new ImageWMS({
            url: config.wmsUrl,
            params: {
                LAYERS: config.wmsLayer,
                VERSION: "1.1.1",
                FORMAT: "image/png",
                TILED: false,
                TRANSPARENT: true,
            },
            serverType: "geoserver",
        }),
        opacity: 0.7,
    });

    map.addLayer(wmsLayer);
    console.log("[MapCanvas] WMS layer added");

    // Initialize handlers
    const categoryHandler = new CategoryHandler(map);
    const featureSelectionHandler = new FeatureSelectionHandler(
        map,
        categoryHandler.getCategorySource(),
    );

    console.log("[MapCanvas] Handlers initialized");

    // CRS Toggle functionality
    function updateCRSButton(): void {
        const button = document.getElementById("crs-toggle-button");
        if (!button) return;

        const state = mapState.getState();

        if (state.localCRS) {
            (button as HTMLButtonElement).disabled = false;
            if (state.activeCRS === state.localCRS) {
                button.textContent = "CRS: UTM";
                button.title = "Zu generischem Koordinatensystem wechseln";
            } else {
                button.textContent = "CRS: generisch";
                button.title = "Zu lokalem UTM-Koordinatensystem wechseln";
            }
        } else {
            (button as HTMLButtonElement).disabled = true;
            button.textContent = "CRS: generisch";
            button.title = "Nur generisches Koordinatensystem verfügbar";
        }
    }

    function toggleCRS(): void {
        const state = mapState.getState();
        if (!state.localCRS) return;

        const targetCRS =
            state.activeCRS === state.localCRS
                ? mapState.getConfig().defaultCRS
                : state.localCRS;

        // Update state
        mapState.setActiveCRS(targetCRS);

        // Use scale-preserving view transition
        toNewViewPreservingScale(map, targetCRS);

        // Refresh categories and WMS
        categoryHandler.refreshCategoriesSource();

        map.once("moveend", () => {
            try {
                wmsLayer.getSource()?.refresh();
            } catch (error) {
                throttledLogger.warn(
                    "wms-refresh",
                    "[WMS] Could not refresh layer",
                    error,
                );
            }
        });

        updateCRSButton();
    }

    // Event listener for CRS toggle button
    document
        .getElementById("crs-toggle-button")
        ?.addEventListener("click", toggleCRS);

    // Subscribe to map state changes
    mapState.subscribe((state) => {
        updateCRSButton();
    });

    // Event listener for kommunen focus
    if (typeof window !== "undefined") {
        window.addEventListener(
            "kommunen:focus",
            (e) => {
                const d = (e as CustomEvent)?.detail || {};
                const view = map?.getView?.();

                if (!view || !view.getProjection) {
                    console.error("[kommunen] map view is not initialized");
                    return;
                }

                // Set local CRS from detail if available
                const localCRS = (d?.projection as string) || undefined;
                mapState.setLocalCRS(localCRS);

                // Register UTM projection if needed
                if (localCRS) {
                    try {
                        registerUtm(localCRS);
                    } catch (error) {
                        throttledLogger.warn(
                            "projection-registration",
                            `Could not register projection ${localCRS}`,
                            error,
                        );
                        mapState.setLocalCRS(undefined);
                    }
                }

                // Set active CRS - prefer local CRS if available
                const targetCRS = localCRS || mapState.getConfig().defaultCRS;
                mapState.setActiveCRS(targetCRS);

                // Create new view with appropriate projection
                toNewViewPreservingScale(map, targetCRS);

                // Refresh WMS and categories after view transition
                map.once("moveend", () => {
                    try {
                        wmsLayer.getSource()?.refresh();
                        categoryHandler.refreshCategoriesSource();
                    } catch (error) {
                        throttledLogger.warn(
                            "refresh-after-focus",
                            "Could not refresh layers",
                            error,
                        );
                    }
                });

                // Handle navigation
                const center = isValidWgs84Coordinate(d?.center)
                    ? d.center
                    : undefined;
                const extent = isValidWgs84Extent(d?.extent)
                    ? d.extent
                    : undefined;
                const zoom = Number.isFinite(d?.zoom) ? d.zoom : undefined;

                try {
                    if (extent && transformExtent) {
                        const fitExtent = transformExtent(
                            extent,
                            "EPSG:4326",
                            targetCRS,
                        );
                        const fitOpts = {
                            padding: [50, 50, 50, 50],
                            duration: 300,
                            ...(d.extra?.fitOptions || {}),
                        };

                        map.getView().fit(fitExtent, fitOpts);
                    } else if (center && transform) {
                        const c = transform(center, "EPSG:4326", targetCRS);
                        const targetZoom =
                            zoom ?? map.getView()?.getZoom?.() ?? 11;

                        map.getView().animate({
                            center: c,
                            zoom: targetZoom,
                            duration: 300,
                            ...(d.extra || {}),
                        });
                    } else {
                        throttledLogger.warn(
                            "invalid-data",
                            "[kommunen] no valid center or extent data provided",
                            d,
                        );
                    }
                } catch (err) {
                    console.error("[kommunen] focus error", err, d);
                }
            },
            { passive: true },
        );
    }

    // Restore persisted state on DOMContentLoaded
    document.addEventListener("DOMContentLoaded", () => {
        console.log("[MapCanvas] DOMContentLoaded event received");
        try {
            // Restore map state from localStorage
            mapState.restoreFromStorage();

            const state = mapState.getState();
            const savedDetail = localStorage.getItem(
                "selectedMunicipalityDetail",
            );

            if (
                state.activeCRS !== mapState.getConfig().defaultCRS &&
                savedDetail
            ) {
                const detail = JSON.parse(savedDetail);
                const localCRS =
                    (detail?.projection as string) &&
                    detail.projection !== "EPSG:4326"
                        ? detail.projection
                        : undefined;

                mapState.setLocalCRS(localCRS);

                if (localCRS) {
                    registerUtm(localCRS);
                }

                // Update view projection
                toNewViewPreservingScale(map, state.activeCRS);

                // Refresh layers after view transition
                map.once("moveend", () => {
                    try {
                        wmsLayer.getSource()?.refresh();
                    } catch (error) {
                        throttledLogger.warn(
                            "wms-refresh-restore",
                            "[WMS] Could not refresh layer",
                            error,
                        );
                    }
                });

                // Dispatch focus event after map initialization
                setTimeout(() => {
                    window.dispatchEvent(
                        new CustomEvent("kommunen:focus", { detail }),
                    );
                }, 100);
            }

            // Reselect feature after categories are loaded
            setTimeout(() => {
                featureSelectionHandler.reselectFeature();
            }, 500);

            // Mark as initialized
            mapState.setInitialized(true);
            console.log("[MapCanvas] Map state fully initialized");
        } catch (error) {
            console.error("Error restoring persisted state:", error);
        }
    });

    // Initialize toggle button state
    updateCRSButton();

    // Check if map container is visible and has dimensions
    setTimeout(() => {
        const mapElement = document.getElementById("map");
        if (mapElement) {
            console.log("[MapCanvas] Map container dimensions:", {
                width: mapElement.offsetWidth,
                height: mapElement.offsetHeight,
                visible: mapElement.offsetParent !== null,
            });

            // Force map update if container was initially hidden
            if (mapElement.offsetWidth > 0 && mapElement.offsetHeight > 0) {
                map.updateSize();
                console.log("[MapCanvas] Map size updated");
            }
        }
    }, 1000);
</script>
