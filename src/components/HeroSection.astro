---
import { Markdown } from 'astro/components';
import heroContent from '../content/hero.md';
---

<section class="relative w-full h-[70vh] flex items-center justify-center overflow-hidden">
  <video
    class="absolute inset-0 w-full h-full object-cover z-0"
    src="/videos/hero-bg.mp4"
    autoplay
    loop
    muted
    playsinline
    poster="/images/hero-fallback.jpg"
  ></video>
  <div class="relative z-10 flex flex-col items-center justify-center text-center text-white bg-black/40 p-8 rounded-lg max-w-3xl mx-auto">
    <Markdown content={heroContent} />
    <!-- Optional: Button, falls im Markdown ein Button-Text angegeben ist -->
  </div>
</section>
:white