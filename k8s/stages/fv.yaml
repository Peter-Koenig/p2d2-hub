apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: p2d2-f-fv-code
  namespace: cc-prd-geodata-stack
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: p2d2-f-fv-config
  namespace: cc-prd-geodata-stack
data:
  DB_USER: P2D2-FV
  WFST_WORKSPACE: fv
  PUBLIC_WFST_WORKSPACE: fv
  PUBLIC_SITE_URL: https://f-fv.udp.data-dna.eu
  # Server-seitiger WFS-T-Endpoint je Stage (mit Workspace-Pfad).
  WFST_ENDPOINT: https://geoportal.udp.data-dna.eu/geoserver/fv/ows
  WFST_ENDPOINT_FV: https://geoportal.udp.data-dna.eu/geoserver/fv/ows
  WFST_USERNAME: p2d2_wfst_fv
  WFST_USER_FV: p2d2_wfst_fv
---
apiVersion: v1
kind: Secret
metadata:
  name: p2d2-f-fv-secret
  namespace: cc-prd-geodata-stack
type: Opaque
stringData:
  # K3: Cluster-DB-Rolle P2D2-FV (Rotation durch Peter)
  DB_PASSWORD: CHANGEME
  # K1: GeoServer-WFS-T-Secret p2d2-geoserver-wfst-fv
  WFST_PASSWORD: CHANGEME
  WFST_PW_FV: CHANGEME
  # K2: zufällig generiert, bleibt
  SESSION_SECRET: CHANGEME
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: p2d2-f-fv
  namespace: cc-prd-geodata-stack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: p2d2-f-fv
  template:
    metadata:
      labels:
        app: p2d2-f-fv
    spec:
      containers:
        - name: astro
          image: node:20-slim
          imagePullPolicy: IfNotPresent
          workingDir: /app
          command: ["node", "dist/server/entry.mjs"]
          ports:
            - containerPort: 4321
          envFrom:
            - configMapRef:
                name: p2d2-base-config
            - secretRef:
                name: p2d2-base-secret
            - configMapRef:
                name: p2d2-f-fv-config
            - secretRef:
                name: p2d2-f-fv-secret
          env:
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: "4321"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: code
              mountPath: /app
      volumes:
        - name: code
          persistentVolumeClaim:
            claimName: p2d2-f-fv-code
---
apiVersion: v1
kind: Service
metadata:
  name: p2d2-f-fv
  namespace: cc-prd-geodata-stack
spec:
  type: ClusterIP
  selector:
    app: p2d2-f-fv
  ports:
    - name: http
      port: 80
      targetPort: 4321
